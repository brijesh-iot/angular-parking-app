{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    //"types": ["node"]
    "types": ["node"] // <-- add "node" here
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
